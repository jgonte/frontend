/**
 * Describes the configurator of the component
 */
 export default interface CustomElementComponentMetadata {

    /**
     * Whether to create a shadow DOM for the component
     * Defaults to true
     */
    shadow: boolean;
}