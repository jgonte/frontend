/**
 * Defines a resource that provides the default text if no key is provided
 * and the key to get the translated text
 */

export default interface IntlResource {

    /**
     * The default text of the resource if no key is provided
     */
     text: string;
}