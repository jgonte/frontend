import Validator from "../Validator";
export default class RecordValidator extends Validator {
    getData(context) {
        return context.dataProvider.getData();
    }
}
//# sourceMappingURL=RecordValidator.js.map